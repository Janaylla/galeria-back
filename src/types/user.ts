export type userInputDTO = {
    name: string
    nickname: string
    password: string
    email: string
};

export type userCredentials = {
    login: string
    password: string
};
export type AuthenticationData = {
    id: string
}
